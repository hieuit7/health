image: ubuntu:14.04
before_script:
  - apt-get update -qq && apt-get install -y curl
  - curl -sL https://deb.nodesource.com/setup_5.10.1 | sudo -E bash -
  - sudo apt-get install -y nodejs
  - npm install 
stages:
  - build
  - test
job1:
  stage: test
  script:
    - mocha
  tags:
    - docker